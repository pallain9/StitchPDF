// Placeholder for text extraction logic using pdfjs-dist
async function extractText(file, { x, y, width, height, unit, page }) {
  return {
    rawText: '',
    logSummary: 'Extraction not implemented yet'
  };
}
module.exports = { extractText };